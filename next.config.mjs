/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // swcMinify is now true by default, so we remove it
  // eslint is now handled by its own config file, so we remove it
};

export default nextConfig;